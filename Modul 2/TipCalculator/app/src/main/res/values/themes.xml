<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TipCalculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/turquoise</item>

    </style>

    <style name="Theme.TipCalculator" parent="Base.Theme.TipCalculator" />

    <style name="Theme.TipCalculator.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/tip</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.TipCalculator</item>
        <item name="windowSplashScreenBackground">@color/white</item>
    </style>
</resources>